<section class="sort-list">
  <header>
    <h1>Sort a list, it's all the same.</h1>
  </header>

  <main>
    {{#ui-dragzone swap="swapBebes"}}
      {{#each bebe in this enabled=true}}
        {{#ui-draggable model=bebe}}
          <img draggable="false" {{bind-attr src=bebe.url}}>
          <span>{{bebe.name}}</span>
        {{/ui-draggable}}
      {{/each}}
    {{/ui-dragzone}}
  </main>
</section>
